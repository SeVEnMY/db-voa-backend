spring:
  datasource:
    url: jdbc:mysql://localhost:3306/voa?useSSL=false&characterEncoding=utf8&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password:
    max-idle: 10
    max-wait: 10000
    min-idle: 5
    initial-size: 5
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher

### Swagger open or not.
swagger:
  enable: true